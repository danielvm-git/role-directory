# Environment Configuration Template
# Copy this file to .env.local and update with your actual values
# Command: cp .env.example .env.local
# IMPORTANT: .env.local is gitignored - never commit credentials to git

# ============================================================================
# DATABASE CONFIGURATION (Required)
# ============================================================================
# Get your Neon DATABASE_URL from:
# https://console.neon.tech/app/projects
# Select: role_directory_dev database -> Connection Details -> Connection String
# Format: postgresql://user:password@ep-xxx.region.neon.tech/role_directory_dev?sslmode=require
DATABASE_URL=postgresql://user:password@ep-xxx-xxx-xxx.us-east-2.aws.neon.tech/role_directory_dev?sslmode=require

# ============================================================================
# AUTHENTICATION CONFIGURATION (Required)
# ============================================================================
# Comma-separated list of allowed email addresses for access control
# Add your email address here for testing
# Example: ALLOWED_EMAILS=user1@example.com,user2@example.com
ALLOWED_EMAILS=your-email@example.com

# ============================================================================
# ENVIRONMENT CONFIGURATION (Optional - defaults provided)
# ============================================================================
# Application environment: development | staging | production
# Default: development
NODE_ENV=development

# Server port number (1-65535)
# Default: 8080 (Cloud Run), 3000 (local)
PORT=3000

# ============================================================================
# NEON AUTH CONFIGURATION (Optional - Epic 3)
# ============================================================================
# Uncomment when implementing Epic 3: Authentication & Access Control
# Get these values from Neon Auth Console
# NEON_AUTH_PROJECT_ID=your-project-id
# NEON_AUTH_SECRET_KEY=your-secret-key

# ============================================================================
# RUNTIME CONFIGURATION (Optional)
# ============================================================================
# Public API URL for client-side requests
# Uncomment if needed for frontend API calls
# NEXT_PUBLIC_API_URL=http://localhost:3000
